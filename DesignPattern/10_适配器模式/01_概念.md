适配器模式是一种结构型设计模式，将一个类的接口转换成客户希望的另一个接口
分类：
①类适配器模式
    定义:
    特点:
②对象适配器模式
    定义:
    特点:
③接口适配器模式
    定义:
    特点:

核心思想
把一个类的接口变换成客户端所期待的另一种接口，从而使原本因接口不匹配而无法在一起工作的两个类能够在一起工作

核心角色
①目标接口：
    定义客户端使用的目标接口，客户端通过调用这个接口中的方法来访问适配器的功能
②适配器：实现目标接口，并在内部持有一个被适配器的实例，适配器将客户端的请求委派给被适配者来完成具体的功能
③被适配者：定义了一个已经存在的接口，但与目标接口不兼容，需要被适配器进行转换

应用场景
①旧系统接口升级
②第三方库集成
③多种数据库操作
④插件系统
⑤硬件接口适配

优点：
①安全可靠
②提高服用性
③扩展性好
④目标和被适配器是完全解耦的关系

缺点：
①过多的适配器会导致系统结构复杂
②如果适配器没有实现好，可能会拖慢整个系统的性能
③滥用适配器模式会导致系统设计紊乱
④适配器模式需要增加一个额外的适配器类，增加了代码的量