# Linux 锁机制笔记：互斥锁与读写锁

## 12. 读写锁（Read-Write Lock）

读写锁适用于“**读多写少**”场景。

特点：
- 多个线程可以同时读
- 写操作必须独占
- 写线程执行时，其他读写线程都要等待

读写锁类型：
```c
pthread_rwlock_t
```

### 12.1 读写锁的特点
- **读锁**：共享锁，多个线程可同时持有
- **写锁**：独占锁，同一时刻只允许一个线程持有
- 有线程持有写锁时，其他线程不能加读锁或写锁
- 有线程持有读锁时，其他线程可以继续读，但不能写

### 12.2 适用场景
适合：
- 配置读取
- 缓存读取
- 查询远多于更新的数据结构

不适合：
- 写操作频繁
- 临界区很短，锁开销明显

### 12.3 相关函数

#### 初始化
```c
int pthread_rwlock_init(pthread_rwlock_t *rwlock,
                        const pthread_rwlockattr_t *attr);
```

#### 加读锁
```c
int pthread_rwlock_rdlock(pthread_rwlock_t *rwlock);
```

#### 加写锁
```c
int pthread_rwlock_wrlock(pthread_rwlock_t *rwlock);
```

#### 解锁
```c
int pthread_rwlock_unlock(pthread_rwlock_t *rwlock);
```

#### 销毁
```c
int pthread_rwlock_destroy(pthread_rwlock_t *rwlock);
```

### 12.4 关于优先级的说明
“写锁优先级一定高于读锁”这种说法并不严谨。  
POSIX 没有统一规定“读优先”还是“写优先”，具体行为取决于实现。

更准确地说：
- 如果当前有线程持有写锁，其他线程都要等待
- 等待中的读线程和写线程谁先获得锁，取决于具体实现策略

### 12.5 示例代码
```c
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <pthread.h>

int number = 0;
pthread_rwlock_t rwlock;

void* read_task(void* arg)
{
    for (int i = 0; i < 5; i++)
    {
        pthread_rwlock_rdlock(&rwlock);
        printf("读线程 %lu, number = %d\n",
               (unsigned long)pthread_self(), number);
        pthread_rwlock_unlock(&rwlock);

        usleep(100000);
    }
    return NULL;
}

void* write_task(void* arg)
{
    for (int i = 0; i < 5; i++)
    {
        pthread_rwlock_wrlock(&rwlock);
        number++;
        printf("写线程 %lu, number = %d\n",
               (unsigned long)pthread_self(), number);
        pthread_rwlock_unlock(&rwlock);

        usleep(150000);
    }
    return NULL;
}

int main()
{
    pthread_t r1, r2, w1;

    pthread_rwlock_init(&rwlock, NULL);

    pthread_create(&r1, NULL, read_task, NULL);
    pthread_create(&r2, NULL, read_task, NULL);
    pthread_create(&w1, NULL, write_task, NULL);

    pthread_join(r1, NULL);
    pthread_join(r2, NULL);
    pthread_join(w1, NULL);

    pthread_rwlock_destroy(&rwlock);

    return 0;
}
```

---

## 15.1 互斥锁（mutex）相关 API 总结

互斥锁用于保护临界区资源，避免多个线程同时访问同一份数据。

### 1. `pthread_mutex_init`
```c
int pthread_mutex_init(pthread_mutex_t *mutex,
                       const pthread_mutexattr_t *attr);
```

作用：
- 初始化互斥锁

示例：
```c
pthread_mutex_t lock;
pthread_mutex_init(&lock, NULL);
```

### 2. `pthread_mutex_destroy`
```c
int pthread_mutex_destroy(pthread_mutex_t *mutex);
```

作用：
- 销毁互斥锁，释放资源

### 3. `pthread_mutex_lock`
```c
int pthread_mutex_lock(pthread_mutex_t *mutex);
```

作用：
- 加锁
- 如果锁已被占用，则阻塞等待

### 4. `pthread_mutex_trylock`
```c
int pthread_mutex_trylock(pthread_mutex_t *mutex);
```

作用：
- 尝试加锁，但不阻塞
- 如果锁不可用，立即返回

### 5. `pthread_mutex_unlock`
```c
int pthread_mutex_unlock(pthread_mutex_t *mutex);
```

作用：
- 解锁
- 释放当前线程持有的锁

### 互斥锁整体流程
```c
pthread_mutex_t lock;
pthread_mutex_init(&lock, NULL);

pthread_mutex_lock(&lock);
/* 访问共享资源 */
pthread_mutex_unlock(&lock);

pthread_mutex_destroy(&lock);
```

### 互斥锁适合的场景
- 多线程修改同一份数据
- 需要严格保证同一时刻只有一个线程访问临界区

---

## 15.2 读写锁（rwlock）相关 API 总结

读写锁允许：
- 多个线程同时读
- 写线程独占

### 1. `pthread_rwlock_init`
```c
int pthread_rwlock_init(pthread_rwlock_t *rwlock,
                        const pthread_rwlockattr_t *attr);
```

### 2. `pthread_rwlock_destroy`
```c
int pthread_rwlock_destroy(pthread_rwlock_t *rwlock);
```

### 3. `pthread_rwlock_rdlock`
```c
int pthread_rwlock_rdlock(pthread_rwlock_t *rwlock);
```

### 4. `pthread_rwlock_wrlock`
```c
int pthread_rwlock_wrlock(pthread_rwlock_t *rwlock);
```

### 5. `pthread_rwlock_unlock`
```c
int pthread_rwlock_unlock(pthread_rwlock_t *rwlock);
```

### 读写锁整体流程
```c
pthread_rwlock_t rwlock;
pthread_rwlock_init(&rwlock, NULL);

pthread_rwlock_rdlock(&rwlock);
/* 读共享资源 */
pthread_rwlock_unlock(&rwlock);

pthread_rwlock_wrlock(&rwlock);
/* 写共享资源 */
pthread_rwlock_unlock(&rwlock);

pthread_rwlock_destroy(&rwlock);
```

### 读写锁适合的场景
- 查询多、修改少
- 希望提高并发读性能

---

## 互斥锁与读写锁对比

| 锁类型 | 读操作 | 写操作 | 适用场景 |
|---|---|---|---|
| 互斥锁 `mutex` | 读也独占 | 写也独占 | 读写都频繁，逻辑简单 |
| 读写锁 `rwlock` | 可多个线程同时读 | 写时必须独占 | 读多写少 |

---

## 读写锁使用代码示例
```c
#define _XOPEN_SOURCE 500
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <pthread.h>
#include <time.h>

#define MAX 50
int number;

pthread_rwlock_t rwlock;

void* read_num(void* arg)
{
    for (int i = 0; i < MAX; i++)
    {
        pthread_rwlock_rdlock(&rwlock);
        printf("Thread read, id = %ld, number = %d\n", pthread_self(), number);
        pthread_rwlock_unlock(&rwlock);
        usleep(rand() % 200);
    }
    return NULL;
}

void* write_num(void* arg)
{
    for (int i = 0; i < MAX; i++)
    {
        pthread_rwlock_wrlock(&rwlock);
        int cur = number;
        cur++;
        number = cur;
        printf("Thread write, id = %ld, number = %d\n", pthread_self(), number);
        pthread_rwlock_unlock(&rwlock);
        usleep(rand() % 10);
    }
    return NULL;
}

int main()
{
    srand(time(NULL));

    pthread_t p1[5], p2[3];
    pthread_rwlock_init(&rwlock, NULL);

    for (int i = 0; i < 5; i++)
    {
        pthread_create(&p1[i], NULL, read_num, NULL);
    }

    for (int i = 0; i < 3; i++)
    {
        pthread_create(&p2[i], NULL, write_num, NULL);
    }

    for (int i = 0; i < 3; i++)
    {
        pthread_join(p2[i], NULL);
    }

    for (int i = 0; i < 5; i++)
    {
        pthread_join(p1[i], NULL);
    }

    pthread_rwlock_destroy(&rwlock);

    printf("Final number = %d\n", number);
    return 0;
}
```

---

## 本文件小结
本文件主要包含：
- 读写锁基础概念
- 互斥锁 API 总结
- 读写锁 API 总结
- 二者使用场景与代码示例